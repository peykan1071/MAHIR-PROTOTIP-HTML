pdfplumber>=0.11,<1
openpyxl>=3.1,<4
python-docx>=1.1,<2
# Web backend (backend/run_file_receiver.py) ve testler yalnız bunları ister.
# Yapay zekâ katmanının (Qdrant, bge-m3, reranker, PaddleOCR-VL, llama-server
# istemcisi) bağımlılıkları local/requirements.txt'te - ayrı ortam, testler
# onları hiç import etmez (local/ modülleri ağır paketleri tembel yükler).
